# 两相交错非同步 Boost 输出纹波 — 数学推导

## 目录

1. [基本 Boost 拓扑 — 占空比](#1-基本-boost-拓扑--占空比)
2. [电流纹波 ΔI_L](#2-电流纹波-δil)
3. [CCM / DCM 判定](#3-ccm--dcm-判定)
4. [交错并联 — 180° 相移](#4-交错并联--180-相移)
5. [二极管电流波形（单相）](#5-二极管电流波形单相)
6. [总二极管电流 — KCL](#6-总二极管电流--kcl)
7. [电容电压纹波 V_C(t)](#7-电容电压纹波-vct)
8. [ESR 电压纹波 V_ESR(t)](#8-esr-电压纹波-vesrt)
9. [总输出纹波](#9-总输出纹波)
10. [交错纹波抵消的数学解释](#10-交错纹波抵消的数学解释)

---

## 1. 基本 Boost 拓扑 — 占空比

Boost 变换器的核心关系：

**MOSFET 导通期间**（时间 DT）：
- 电感两端电压 = V_in
- 电感电流线性上升：diL/dt = V_in / L
- 二极管截止，负载完全靠输出电容供电

**MOSFET 关断期间**（时间 (1-D)T）：
- 电感两端电压 = V_in - (V_out + V_d)
- 电感电流线性下降：diL/dt = (V_in - V_out - V_d) / L
- 二极管导通，电感向负载和电容传递能量

### 伏秒平衡

稳态下电感电流净变化为零：

```
V_in × DT + (V_in - V_out - V_d) × (1-D)T = 0
```

化简：

```
V_in × D = (V_out + V_d) × (1-D)
```

解出占空比（含效率 η 修正）：

```
D = 1 - (V_in × η) / (V_out + V_d)
```

> 效率 η 的引入：实际电路中 V_out × I_out = V_in × I_in × η，有效电压传输比需乘以 η。

---

## 2. 电流纹波 ΔI_L

电感在导通期间的电流上升量：

```
ΔI_L = V_in × D × T / L = V_in × D / (f_sw × L)
```

这是三角波纹波的峰峰值，由 V_in（导通期间电感电压）、D（占空比）、L（电感值）共同决定。

---

## 3. CCM / DCM 判定

**CCM（连续导通模式）** 条件：电感平均电流大于纹波电流的一半

```
I_avg > ΔI_L / 2   →   CCM
I_avg ≤ ΔI_L / 2   →   DCM
```

直观理解：如果平均电流够大，电感电流在关断结束前不会降到零，就是 CCM；否则电流断续，进入 DCM。

---

## 4. 交错并联 — 180° 相移

两相交错的核心思想：

```
Phase 1: 在 t = 0 开始导通
Phase 2: 在 t = T/2 开始导通（180° 延迟）
```

总二极管电流叠加：

```
I_d_total(t) = I_d1(t) + I_d2(t + T/2)
```

**为什么交错能降低纹波？** 两相的电流波形错开半个周期叠加，当占空比 D ≈ 50% 时，两相纹波峰谷恰好对消，总纹波最小。

---

## 5. 二极管电流波形（单相）

### 5.1 CCM 模式

一个开关周期内，二极管电流波形是三角波：

| 区间 | 状态 | 二极管电流 |
|------|------|-----------|
| [0, DT] | MOSFET 导通 | Id = 0 |
| [DT, T] | MOSFET 关断 | Id 从 I_peak 线性降到 I_valley |

其中：

```
I_peak  = I_avg + ΔI_L / 2
I_valley = I_avg - ΔI_L / 2
```

在 [DT, T] 区间内，任意时刻 t 的二极管电流：

```
Id(t) = I_peak - [(t - DT) / (T - DT)] × (I_peak - I_valley)
```

### 5.2 DCM 模式

电流断续，波形为三角脉冲：

| 区间 | 状态 | 二极管电流 |
|------|------|-----------|
| [0, T_on] | MOSFET 导通 | Id = 0 |
| [T_on, T_on+T_off2] | 二极管导通 | Id 从 I_peak 线性降到 0 |
| [T_on+T_off2, T] | 死区时间 | Id = 0 |

### 5.3 DCM 峰值电流推导

由三个方程联立求解：

**① 平均电流 = 面积 / 周期：**

```
I_avg = (1/2 × I_peak × (T_on + T_off2)) / T
```

**② 导通期间电流上升：**

```
I_peak = V_in × T_on / L    →    T_on = I_peak × L / V_in
```

**③ 关断期间电流下降到零：**

```
I_peak = (V_out + V_d - V_in) × T_off2 / L    →    T_off2 = I_peak × L / (V_out + V_d - V_in)
```

将 ②③ 代入 ①，定义：

```
k = L × (1/V_in + 1/(V_out + V_d - V_in)) / T
```

解出峰值电流：

```
I_peak = √(2 × I_avg / k)
```

---

## 6. 总二极管电流 — KCL

输出节点的电流关系（基尔霍夫电流定律）：

```
I_d_total(t) = I_d1(t) + I_d2(t)     ← 两相二极管电流之和
I_c(t) = I_d_total(t) - I_out         ← 电容电流 = 总二极管电流 - 负载电流
```

**物理含义**：二极管电流一部分供给负载（恒定 I_out），多余/不足的部分由电容吸收/释放。

---

## 7. 电容电压纹波 V_C(t)

电容的基本关系：I = C × dV/dt，因此：

```
V_C(t) = (1/C) × ∫₀ᵗ I_c(τ) dτ
```

离散实现（累加近似积分）：

```
V_C[i] = V_C[i-1] + I_c[i] × dt / C
```

然后减去一个周期的均值，消除直流偏移，只保留交流纹波分量。

---

## 8. ESR 电压纹波 V_ESR(t)

输出电容的等效串联电阻（ESR）产生与电流成正比的压降：

```
V_ESR(t) = I_c(t) × ESR
```

这是一项纯阻性压降，波形和电容电流完全同相。

---

## 9. 总输出纹波

```
V_ripple(t) = V_C(t) + V_ESR(t)
```

峰峰值：

```
V_pp = max(V_ripple) - min(V_ripple)
```

### 纹波分量占比

| 条件 | 主导分量 |
|------|---------|
| 高频 / 小电容 | 电容纹波 V_C |
| 大 ESR（电解电容） | ESR 纹波 |
| 陶瓷电容（ESR ≈ 几 mΩ） | V_C 主导 |

---

## 10. 交错纹波抵消的数学解释

对于两相交错（相移 180°），总二极管电流的纹波分量：

```
ΔI_total(t) = ΔI₁(t) + ΔI₂(t + T/2)
```

### 关键结论：D = 0.5 时纹波理论为零

当 D = 0.5 时：
- Phase 1 导通区间：[0, 0.5T]
- Phase 2 导通区间：[0.5T, T]
- 两相的纹波峰谷完全错开 → 对消

### 偏离 D = 0.5 时

残余纹波大小取决于 |D - 0.5|：
- D = 0.3 或 0.7 时，对消不完全
- D = 0.25 或 0.75 时，叠加反而可能增大

### 工程意义

如果 V_in/V_out 比值使得 D ≈ 0.5（即 V_out ≈ 2 × V_in），交错拓扑的纹波抑制效果最好。

---

## 附录：代码对应关系

| 公式 | 代码变量 |
|------|---------|
| D = 1 - V_in×η/(V_out+V_d) | `D = 1 - (Vin * eta) / (Vout + Vd)` |
| ΔI_L = V_in×D×T/L | `dIL = (Vin * D * T) / L` |
| I_avg > ΔI_L/2 → CCM | `isCCM = Iavg > dI / 2` |
| I_peak(DCM) = √(2×I_avg/k) | `Ipeak = Math.sqrt(2 * Iavg / k)` |
| V_C = (1/C)∫I_c dt | `Vc[i] = sum / Cout` |
| V_ESR = I_c × ESR | `Vesr[i] = Ic[i] * ESR` |
| 180° 相移 | `Id2[i] = ph2.Id[(i + shift) % N]` |

---

*配套工具：[两相交错非同步 Boost 输出纹波计算器](https://zzsscc1001.github.io/my-page/)*
